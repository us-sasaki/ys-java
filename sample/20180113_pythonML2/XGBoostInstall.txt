2018/1/15

Windows10 上に MinGW64 をインストール(x86_64, posix thread を選択)
xgboost の git レポジトリをレポジトリ上の installation に従ってコンパイル
できた xgboost.dll を python-package/xgboost にコピーし、
python setup.py install を実行。

その後、xgboost フォルダを消せなくなってしまった。

dll のみをコピーして setup.py してもだめ。

当面、MinGW64, git clone した xgboost は消さないでおく。
